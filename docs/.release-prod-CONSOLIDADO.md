---
description: Release segura dev → main
---

# 🚀 Workflow: Release Produção

## Quando Usar
Merge da branch `dev` para `main` (produção).

## Checklist de Segurança

### 1. Garantias (dev)
```bash
git switch dev && git pull
npm run test:quick  # Valida integridade básica
```

### 2. Versionamento
Ver `/versionamento` para bump de versão

### 3. Merge dev → main
```bash
git switch main
git pull origin main
git merge dev --no-ff  # Merge explícito
```

### 4. Push & Tag
```bash
git push origin main
git push origin tags  # Envia todas as tags
```

---

**⚠️ NUNCA**: Commitar direto em `main`  
**⚠️ SEMPRE**: Passar por `dev` primeiro

**Refs**: `/versionamento`, `/git-flow`
