---
description: Enterprise Protocol para bump de versão consistente
---

# 🔢 Workflow: Versionamento

## Quando Usar
Atualizar versão do projeto (semantic versioning).

## Passos

### 1. Determinar Tipo de Versão
- **Patch** (2.9.6 → 2.9.7): Bugfixes
- **Minor** (2.9.x → 2.10.0): Features
- **Major** (2.x.x → 3.0.0): Breaking changes

### 2. Enterprise Protocol (Tripla Sincronização)
```bash
# Atualizar 3 arquivos:
# 1. package.json
# 2. manifest.json  
# 3. CHANGELOG.md (seção "Unreleased" → versão)
```

### 3. Commit
```
chore(release): bump version to vX.Y.Z
```

### 4. Tag Git
```bash
git tag -a vX.Y.Z -m "Release vX.Y.Z"
git push origin vX.Y.Z
```

---

**Refs**: `CHANGELOG.md`, `/release-prod`
