/**
 * @typedef {Object} SessionValidationResult
 * @property {boolean} isValid - Se as credenciais são válidas
 * @property {string} fullEmail - Email completo formatado (RA + Domínio)
 * @property {string} cleanDomain - Domínio normalizado
 * @property {string} [error] - Mensagem de erro se inválido
 */

export const SessionModel = {};
